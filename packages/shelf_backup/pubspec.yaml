name: shelf_backup
description: Backup/restore pipelines and .acshelf package format for AppConfigShelf.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

dependencies:
  archive: ^4.0.0
  crypto: ^3.0.0
  glob: ^2.1.0
  path: ^1.9.0
  shelf_core: ^0.1.0
  shelf_rules: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  shelf_win32: ^0.1.0
  test: ^1.25.0
