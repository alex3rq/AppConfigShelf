name: shelf_detect
description: Detection engine for AppConfigShelf - detector providers and evidence resolver. Pure Dart; all I/O behind view interfaces.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

dependencies:
  glob: ^2.1.0
  shelf_core: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
