name: shelf_rules
description: Rule engine for AppConfigShelf - parses and validates database entries and custom items. Single source of truth for db schema v1.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

dependencies:
  shelf_core: ^0.1.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
