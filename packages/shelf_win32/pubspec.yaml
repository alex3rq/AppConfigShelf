name: shelf_win32
description: Windows-specific infrastructure for AppConfigShelf - registry, known folders, process detection. Only package allowed to touch win32 APIs.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

dependencies:
  shelf_core: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
